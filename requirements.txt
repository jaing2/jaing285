# Streamlit Cloud 는 Python 3.14 로 빌드합니다.
# 아래 조합은 전부 사전 컴파일된 cp314 휠이 존재해 소스 빌드가 일어나지 않습니다.
#
# pandas: 2.x 중 cp314 휠이 있는 버전은 2.3.3 뿐입니다.
#         pykrx 1.2.x 가 pandas<3.0 을 요구하므로 상한을 함께 지정합니다.
#         (상한이 없으면 pandas 3.x 가 설치되고, 의존성 해석기가 pykrx 를
#          pkg_resources 를 쓰는 구버전으로 되돌려 임포트가 실패합니다.)

streamlit>=1.60
pykrx>=1.2.8
pandas>=2.3.3,<3
groq>=1.0
